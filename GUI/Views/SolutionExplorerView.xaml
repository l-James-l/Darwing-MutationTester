<UserControl x:Class="GUI.Views.SolutionExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.Views"
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:GUI.ViewModels"
             xmlns:evm="clr-namespace:GUI.ViewModels.SolutionExplorerElements"
             xmlns:views="clr-namespace:GUI.Views.SolutionExplorerElements"
             xmlns:cv="clr-namespace:GUI.Converters"
             d:DataContext="{d:DesignInstance Type=vm:SolutionExplorerViewModel}">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type evm:FileExplorerViewModel}">
            <views:FileExplorerView/>
        </DataTemplate>

        <cv:MutationStatusColourConverter x:Key="MutationStatusColourConverter"/>

        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10,10,0,10"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontWeight="SemiBold" FontSize="26" Text="Solution Explorer"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="150"/> <!--File Tree-->
                <ColumnDefinition Width="2"/> <!--Splitter-->
                <ColumnDefinition Width="3*" MinWidth="300"/> <!--File contents-->
                <ColumnDefinition Width="2"/> <!--Splitter-->
                <ColumnDefinition Width="*" MinWidth="150"/> <!--Mutation details-->
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="50"/> <!--Header row-->
                <RowDefinition Height="*"/> <!--Content-->
            </Grid.RowDefinitions>

            <!-- File Explorer on the left -->
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Solution Structure" Style="{DynamicResource HeaderStyle}"/>
            <ContentControl Grid.Column="0" Grid.Row="1" Content="{Binding FileExplorerViewModel}" />

            <!-- Splitter -->
            <GridSplitter Grid.Column="1"
                          Width="5"
                          Grid.RowSpan="2"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          ResizeBehavior="PreviousAndNext"
                          ResizeDirection="Columns"
                          ShowsPreview="True"/>

            <!-- File Content Panel -->
            <TextBlock Grid.Column="2" Grid.Row="0" Text="{Binding SelectedFileName}" Style="{DynamicResource HeaderStyle}"/>

            <Border Grid.Column="2" Grid.Row="1" Background="#333">
                <DataGrid ItemsSource="{Binding FileDetails}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="None"
                          GridLinesVisibility="None"
                          Background="#0F172A"
                          Foreground="#E2E8F0"
                          BorderThickness="0"
                          SelectionUnit="FullRow"
                          SelectionMode="Single"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Visible">

                    <DataGrid.Resources>

                        <!-- Row style -->
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="MinHeight" Value="20"/>

                            <!-- selection highlight -->
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#1E293B"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#172554"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>

                        <!-- Selected row style -->
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                        </Style>

                    </DataGrid.Resources>

                    <DataGrid.Columns>

                        <!-- Line number -->
                        <DataGridTextColumn Binding="{Binding LineNumber}" Width="55">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontFamily" Value="Cascadia Mono"/>
                                    <Setter Property="Foreground" Value="#64748B"/>
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="Margin" Value="0,0,10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Code -->
                        <DataGridTextColumn Binding="{Binding SourceCode}" Width="Auto">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontFamily" Value="Cascadia Mono"/>
                                    <Setter Property="Foreground" Value="#E2E8F0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ItemsControl ItemsSource="{Binding MutationsOnLine}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="5,0"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Command="{Binding DataContext.SelectMutationCommand, 
                                                                  RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                        CommandParameter="{Binding}"
                                                        Cursor="Hand">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Ellipse x:Name="Circle"
                                                                     Width="8" 
                                                                     Height="8" 
                                                                     Margin="2,0"
                                                                     Fill="{Binding Converter={StaticResource MutationStatusColourConverter}}"
                                                                     ToolTip="{Binding Status}"/>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="Circle" Property="Opacity" Value="0.7"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>

            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="3"
                          Width="5"
                          Grid.RowSpan="2"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          ResizeBehavior="PreviousAndNext"
                          ResizeDirection="Columns"
                          ShowsPreview="True"/>


            <TextBlock Grid.Column="4" Grid.Row="0" Text="Mutation Details" Style="{DynamicResource HeaderStyle}"/>

            <!--Selected Mutation Details Section-->
            <Grid Grid.Column="4" Grid.Row="1">
                <!--No Mutation Selected Display-->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <!--Trigger this display to be visible only when no mutation is selected-->
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedMutation}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <TextBlock Text="No Mutation Selected" TextWrapping="WrapWithOverflow" FontSize="16" Opacity="0.5"/>
                </StackPanel>

                <!--Display the details of the selected mutation-->
                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <!--Trigger this display to be visible only when a mutation is selected-->
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedMutation}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <StackPanel>
                        <TextBlock Text="place holder for selected mutant" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
